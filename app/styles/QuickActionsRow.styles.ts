import { StyleSheet } from "react-native";

export const quickActionsRowStyles = StyleSheet.create({
  quickActionsRow: {
    flexDirection: "row",
    alignItems: "flex-start",
    gap: 12,
    paddingVertical: 10,
  },
});
